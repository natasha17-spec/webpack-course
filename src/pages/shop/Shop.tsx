import React from 'react';
import classes from './App.module.scss'
import {Link, Outlet} from "react-router-dom";

export const Shop = () => {
    return (
        <div>
            Shop
        </div>
    );
};

export default Shop;